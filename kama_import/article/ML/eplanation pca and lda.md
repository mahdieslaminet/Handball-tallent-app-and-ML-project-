# کاهش بُعد داده با PCA و LDA (نمایش دو بعدی)

در این پروژه، یک دیتاست پزشکی انتخاب شده و دو روش کاهش بُعد **PCA** و **LDA** روی آن اعمال شده است.  
هدف، کاهش ابعاد داده به **۲ مؤلفه (۲D)** و نمایش بصری نتایج می‌باشد.

---

## 📊 دیتاست مورد استفاده

**Breast Cancer Wisconsin Dataset**

این دیتاست شامل ویژگی‌های استخراج‌شده از تصاویر توده‌های پستان است و برای مسائل طبقه‌بندی پزشکی بسیار رایج می‌باشد.  
داده‌ها شامل دو کلاس «خوش‌خیم» و «بدخیم» هستند که آن را برای روش LDA مناسب می‌کند.

---

## 🧠 روش‌ها

### 🔹 PCA (تحلیل مؤلفه‌های اصلی)
- بدون استفاده از برچسب‌ها
- هدف: حفظ بیشترین واریانس داده
- کاهش بُعد به ۲ مؤلفه اصلی

### 🔹 LDA (تحلیل تفکیکی خطی)
- استفاده از برچسب‌های کلاس
- هدف: بیشینه‌سازی جداسازی بین کلاس‌ها
- کاهش بُعد به ۲ مؤلفه تفکیکی

---

## 🛠️ مراحل اجرا

1. بارگذاری دیتاست
2. نرمال‌سازی داده‌ها با StandardScaler
3. اجرای PCA با `n_components=2`
4. اجرای LDA با `n_components=2`
5. نمایش بصری نتایج به‌صورت نمودار دوبعدی

---

## 📈 نتایج

- در نمودار PCA، داده‌ها بر اساس بیشترین واریانس توزیع شده‌اند اما همپوشانی کلاس‌ها مشاهده می‌شود.
- در نمودار LDA، جداسازی کلاس‌ها واضح‌تر است، زیرا از اطلاعات برچسب‌ها استفاده شده است.
- برای این دیتاست پزشکی، LDA نمایش بصری بهتری نسبت به PCA ارائه می‌دهد.

---

## 📌 نتیجه‌گیری

PCA بیشتر برای فشرده‌سازی داده و کاهش نویز مناسب است،  
در حالی که LDA برای مسائل طبقه‌بندی و جداسازی کلاس‌ها عملکرد بهتری دارد.

---

## 📚 ابزارها و کتابخانه‌ها

- Python
- scikit-learn
- matplotlib
- numpy

---

## 👤 درس: ماشین لرنینگ  
تمرین: کاهش بُعد و نمایش دوبعدی داده‌ها با PCA و LDA
